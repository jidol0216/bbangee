"""
Constants - Robot Specs, Camera Params, Safety Limits

로봇 및 카메라 관련 상수 정의
모든 단위는 명시적으로 표기 (mm, deg, sec)
"""

# ============================================
# Doosan M0609 Robot Specs
# ============================================

# 조인트 범위 (deg) - Face Tracking용 제한된 범위
# 로봇 풀 스펙이 아닌 안전한 추적 범위
JOINT_LIMITS = {
    'J1': (-15.0, 15.0),     # 베이스 회전 제한 (±15도 - 사용자 요청)
    'J2': (-95.0, 95.0),     # 어깨 (스펙 동일)
    'J3': (-160.0, 160.0),   # 팔꿈치 (스펙 동일)
    'J4': (-360.0, 360.0),   # 손목 피치 (스펙 동일)
    'J5': (-135.0, 135.0),   # 손목 롤 (스펙 동일)
    'J6': (-10.0, 10.0),     # 손목 요 - Face Tracking용 제한 (카메라 롤 안정)
}

# 조인트 속도 제한 (deg/s) - Face Tracking용 추적 속도
# 최적 제어 성능 향상을 위해 속도 증가
JOINT_VEL_MAX = {
    'J1': 15.0,    # 베이스 회전 (25→15, 좌우 흔들림 감소)
    'J2': 18.0,    # 어깨 (35→18)
    'J3': 20.0,    # 팔꿈치 (40→20)
    'J4': 30.0,    # 손목 피치 (60→30)
    'J5': 30.0,    # 손목 롤 (60→30)
    'J6': 20.0,    # 손목 요 (40→20)
}

# 링크 길이 (mm)
LINK_LENGTHS = {
    'L1': 135.0,   # 베이스 높이
    'L2': 411.0,   # 어깨 링크
    'L3': 368.0,   # 팔꿈치 링크
    'L4': 113.0,   # 손목 링크
}

# TCP 오프셋 (mm) - RG2 그리퍼 + 카메라
TCP_OFFSET_Z = 228.6

# ============================================
# Safety Limits (mm)
# ============================================

SAFETY_LIMITS = {
    'r_min': 300.0,    # 최소 반경 - 로봇 몸통 충돌 방지
    'r_max': 850.0,    # 최대 반경 - 도달 한계
    'z_min': 150.0,    # 최소 높이 - 테이블 충돌 방지
    'z_max': 800.0,    # 최대 높이
}

# 안전 거리 (mm) - 얼굴과의 거리
SAFETY_DISTANCE = 650.0

# ============================================
# Camera Params (RealSense D435i)
# ============================================

CAMERA_PARAMS = {
    'frame_id': 'camera_color_optical_frame',
    'width': 640,
    'height': 480,
    'fps': 30,
}

# ============================================
# Control Params
# ============================================

# 제어 주파수 (Hz)
CONTROL_FREQUENCY = {
    'detection': 30,    # 얼굴 감지
    'tracking': 100,    # EKF 추적
    'control': 50,      # 로봇 제어
}

# 데드존 (deg) - 떨림 방지
DEAD_ZONE_DEG = 2.0

# 도착 판정 임계값 (deg)
ARRIVAL_THRESHOLD_DEG = 3.0

# ============================================
# Home / Start Positions (deg) - 카메라 높이 올림
# ============================================

HOME_JOINTS = [0.0, 0.0, 90.0, 0.0, 90.0, 0.0]  # 원본 홈 위치
# J6=188.33° (카메라 180도 회전 장착 반영)
START_JOINTS = [3.06, 2.84, 92.13, 86.07, -1.43, 188.33]  # 원본 시작 위치 (J6만 카메라 회전 반영)

# ============================================
# EKF Params
# ============================================

EKF_PARAMS = {
    'dt': 0.033,           # 샘플링 시간 (30Hz)
    'process_noise': 0.1,  # 프로세스 노이즈
    'measure_noise_3d': 5.0,  # 측정 노이즈 (mm)
    'measure_noise_2d': 2.0,  # 측정 노이즈 (pixel)
}
